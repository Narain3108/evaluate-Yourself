apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "evaluate-yourself.fullname" . }}-test-connection"
  labels:
    {{- include "evaluate-yourself.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "evaluate-yourself.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
